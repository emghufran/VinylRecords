Next Steps

Global setup:
Set up git
git config --global user.name "Muhammad Ghufran"
git config --global user.email emghufran@gmail.com

Next steps:
mkdir VinylRecords
cd VinylRecords
git init
touch README
git add README
git commit -m 'first commit'
git remote add origin git@github.com:emghufran/VinylRecords.git
git push -u origin master

Existing Git Repo?
cd existing_git_repo
git remote add origin git@github.com:emghufran/VinylRecords.git
git push -u origin master
